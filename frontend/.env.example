# URL of your backend server that holds the Gemini API key and proxies chat requests.
# The frontend NEVER talks to Gemini directly — it always calls your backend.
VITE_API_BASE_URL=http:https://ayush-x8wk.onrender.com

